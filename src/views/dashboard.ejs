<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Latest compiled and minified CSS -->
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <title>Url shortener</title>
  </head>
  <body>
      <div class="container">
            <h1>URL shortener</h1>
    <form method="post" action='/shortUrl' class="my-4 form-inline">
      <label for="fullUrl" class="sr-only">Full URL</label>
      <input type="url" placeholder="url" required name="fullUrl" id="urlInput" class="form-control col mr-2 pt-3"/>
      <button type="submit" class="btn btn-success">SHORTEN THIS</button>
    </form>
    <table class="table table-striped table-responsive">
      <thead>
        <tr>
          <th>URL</th>
          <th>Shrinked URL</th>
          <th>Number of clicks :</th>
        </tr>
      </thead>
      <tbody>
          <% shortUrl.forEach(URL=>{%>   
             <tr>
          <td>
            <a href="<%= URL.longUrl   %>"
              ><%= URL.longUrl   %></a
            >
            <td>  <a href="<%=  URL.shortUrls   %>" target="_blank"><%=  URL.shortUrls %></a></td>
          
            <td><%= URL.count%></td>
          </td>
        </tr>  <%})   %>
       
      </tbody>
    </table>
  </body>
      </div>
  
</html>
